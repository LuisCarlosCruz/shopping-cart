import React from 'react';
import CardItens from '../cardItens/CartItens';

const SectionItens = () => {
  return (
    <div>
      {/* asideBar // Criar a lista de categorias */}
      <CardItens />
      {/* Img propaganda Vertical  */}
    </div>
  );
};

export default SectionItens;
